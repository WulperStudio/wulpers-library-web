import { Meta, Story, Preview } from "@storybook/addon-docs/blocks"
import { withKnobs, text, object, boolean } from "@storybook/addon-knobs"
import { action } from "@storybook/addon-actions"
import PrincipalFormPost from "./PrincipalFormPost"

<Meta title="Organisms/Admin/PrincipalFormPost" />

## PrincipalFormPost

Description PrincipalFormPost

<Preview withToolbar>
  <Story name="PrincipalFormPost">
    <PrincipalFormPost
      values={{
        title: "",
        content: "",
        slug: "",
        status: "Draft",
        image: [],
      }}
      errors={{
        title: false,
        content: false,
        slug: false,
        status: false,
        image: false,
      }}
      setErrors={values => action("Errors", values)}
      setValues={values => action("Values", values)}
    />
  </Story>
</Preview>
