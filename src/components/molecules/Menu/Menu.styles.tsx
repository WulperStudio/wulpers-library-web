import {makeStyles} from "@material-ui/core/styles"

const useStyles = makeStyles({
  listItemText: {
    "& .MuiTypography-root": {
      fontSize: "0.875rem"
    }
  }
})

export default useStyles;
