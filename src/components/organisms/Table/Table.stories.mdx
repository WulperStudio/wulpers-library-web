import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, object, boolean } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";
import Table from "./Table";
import {
  formatRows,
  dataRows,
  headCells,
} from "../../organisms/Table/dataMoock";

<Meta title="Organisms/Admin/Table" />

# Table

Description Table

## Table Default

<Preview withToolbar>
  <Story name="Table">
    <Table
      loading={boolean("loading", false)}
      idTable="id"
      selectable={true}
      headCells={object("headCells", headCells)}
      formatRows={object("formatRows", formatRows)}
      dataRows={object("dataRows", dataRows)}
    />
  </Story>
</Preview>
