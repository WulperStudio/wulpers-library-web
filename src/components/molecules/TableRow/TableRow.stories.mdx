import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, boolean } from "@storybook/addon-knobs";
import { action } from '@storybook/addon-actions';
import TableRow from "./TableRow";
import {formatRows, dataRows} from "../../organisms/Table/dataMoock"

<Meta title="Molecules/TableRow" />

# TableRow

Description TableRow

## TableRow Default
```
const dataRows = [
  { name: "Cupcake", calories: 305, fat: 3.7, carbs: 67, protein: 4.3 }, ...
]
```
```
const formatRows = [
  { key: "name" },
  { key: "calories", align: "right", chip: true },
  { key: "fat", align: "right", image: "https://material-ui.com/static/images/avatar/1.jpg" },
  { key: "carbs", align: "right" },
  { key: "protein", align: "right" }
]
```
<Preview withToolbar>
  <Story name="Default">
    <TableRow
      key={0}
      index={0}
      isItemSelected={false}
      formatRows={formatRows}
      dataRow={dataRows[0]}
      isCheckbox={true}
      dataRowKey={dataRows[0].name}
      handleClick={action("handleClick")}
    />
  </Story>
</Preview>
