import {Meta, Story, Preview} from "@storybook/addon-docs/blocks"
import {withKnobs, text, object} from "@storybook/addon-knobs"
import {action} from '@storybook/addon-actions'
import AsideFixed from "./AsideFixed"
import Fab from '@material-ui/core/Fab'
import EditIcon from '@material-ui/icons/Edit'
import Menu from "../../molecules/Menu/Menu"
import TextField from './../../atoms/Form/TextField'
import Typography from "@material-ui/core/Typography"
import Autocomplete from '@material-ui/lab/Autocomplete'
import {autocompleteValues} from "../../atoms/Form/Form.utils"
import Dropzone from "../../atoms/DropZone/Dropzone"
import SiderbarEditionTask from "../../molecules/SiderbarEditionTask"

<Meta title="Containers/AsideFixed"/>

# AsideFixed

Description AsideFixed

## AsideFixed Default
<Preview withToolbar>
  <Story name="Default">
    <AsideFixed asideContent={
      <SiderbarEditionTask />
    }>
      <Typography gutterBottom variant="h6" component="h2" color="primary">
        POST DESCRIPTION -> Title section
      </Typography>
      <p><TextField label="Title" fullWidth defaultValue="Flipp, la aplicación de los dioses"/></p>
      <p><TextField label="Description" fullWidth multiline rows={4}
                    defaultValue="Flipp es una aplicación para otorgar micro creditos de una forma totalmente accesible para cualquier persona desde la comodidad de su celular. Cuentanos que te parece nuestra jerarquia de ..."/>
      </p>
      <p><TextField label="Attach image" fullWidth multiline rows={4}/></p>
      <p><Dropzone/></p>
      <Typography gutterBottom variant="h6" component="h2" color="primary">
        CONTENT -> Title section
      </Typography>
      <p><TextField label="Paragraph" fullWidth multiline rows={4}
                    defaultValue="Hola joséh e leído tu artículo y me ha servidor mucho, ya que, quiero poder capturar las fotos desde una app que estoy desarrollando pero me encuentro con que mi falta de conocimientos me limita y no sé recuperar la foto guardada."/>
      </p>
      <p><TextField label="Attach video" fullWidth multiline rows={4} defaultValue="www.youtube.com/viral-things"/></p>
      <p><Autocomplete
        multiple
        id="tags-standard"
        options={autocompleteValues}
        getOptionLabel={(option) => option.title}
        defaultValue={[autocompleteValues[13]]}
        renderInput={(params) => (
          <TextField
            {...params}
            variant="standard"
            label="Sector"
          />
        )}
      /></p>
    </AsideFixed>
  </Story>
</Preview>
